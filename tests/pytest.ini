[pytest]
asyncio_default_fixture_loop_scope =  session
asyncio_mode = auto
pythonpath = ../app/server
; Определение пути импорта библиотек и метода обработки асинхронных запросов
filterwarnings = ignore::sqlalchemy.exc.SAWarning
; Скрыть предупреждения об повторном использовании запросов (повторный лайк, повторная подписка)
